## Trafsys Data Transfer deployment script

Before running, use `ansible-galaxy collection install community.general` to install ansible's npm module. You may need to run the playbook with `--ask-become-pass`.